{
  "devDependencies": {
    "concurrently": "^9.1.0",
    "tailwindcss": "^3.4.16"
  },
  "scripts": {
    "dev": "concurrently \"npx tailwindcss -i ./input.css -o ./output.css --watch\" \"live-server --no-css-inject\""
  }
}
